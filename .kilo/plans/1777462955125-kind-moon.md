# Plan: Mise a jour des docs .md (TensorTrade + nouveautes)

## Contexte
Les commits des 7 derniers jours ont ajoute plusieurs fonctionnalites qu'aucun fichier .md ne mentionne :
- **TensorTrade model** (`src/tensortrade_model.py`) + 3 fichiers de tests
- **Cache invalidation** automatique si > 2 jours (`src/data.py`)
- **Fallback MA50** quand MA200 insuffisant (`src/data.py`)
- **Scripts utilitaires** : `refresh_cache.py`, `update_decision_engine.py`, `update_enhance_trading.py`
- **Suppression des DB** du tracking git (`performance_monitor.db`, `trading_history.db`)

## Fichiers a mettre a jour (par priorite)

### 1. `productContext.md` — CRITIQUE
- Decrit un systeme a 2 modeles (scikit-learn + LLM) alors qu'il y en a 8+
- Ajouter : ensemble multi-modeles, Oil/WTI, Trading 212, TimesFM, Hyperliquid, Vincent Ganne, Oil-Bench, Sentiment, TensorTrade

### 2. `progress.md` — HAUTE
- Ajouter entrees dans "Corrections Recentess" pour :
  - 2026-04-29: Cache invalidation + fix stale data PROD
  - 2026-04-28: TensorTrade model + tests + benchmarks
  - 2026-04-27: MA50 fallback, refresh_cache.py, update scripts
- Ajouter TensorTrade dans "Ce Qui Fonctionne"
- Mettre a jour les phases (Phase 3/4 dates obsoletes)

### 3. `activeContext.md` — HAUTE
- Ajouter TensorTrade dans "Key Recent Changes"
- Ajouter cache invalidation + MA50 fallback
- Cocher les objectifs realises, ajouter nouveaux objectifs

### 4. `systemPatterns.md` — MOYENNE
- Ajouter TensorTrade dans la liste des signaux du Decision Logic
- Ajouter pattern "Stale Cache Auto-Invalidation"
- Ajouter pattern "MA50 Fallback"

### 5. `techContext.md` — MOYENNE
- Ajouter TensorTrade dans Core Technologies
- Ajouter `tensortrade_model.py`, `refresh_cache.py` dans Key Components
- Ajouter cache invalidation dans Resilience Architecture
- Mettre a jour Monitoring (DB files plus tracks)

### 6. `README.md` — MOYENNE
- Ajouter TensorTrade dans la liste des 8 signaux (devient 9)
- Ajouter `refresh_cache.py` dans le project structure
- Mettre a jour la section Quick Start avec mention du cache
- Retirer/ajuster les references a `trading_history.db`

### 7. `SYSTEM_SUMMARY.md` — MOYENNE
- Ajouter TensorTrade dans la section modeles
- Ajouter section cache invalidation
- Ajouter MA50 fallback dans la section donnees
- Mettre a jour references aux DB files

### 8. `TODO.md` — BASSE
- Cocher "Urea MA200 = nan" si resolu par le fallback MA50
- Ajouter entrees pour TensorTrade, cache invalidation
- Ajouter taches restantes identifiees

## Execution (ordre)
1. `productContext.md` (le plus en retard)
2. `progress.md`
3. `activeContext.md`
4. `systemPatterns.md`
5. `techContext.md`
6. `README.md`
7. `SYSTEM_SUMMARY.md`
8. `TODO.md`
