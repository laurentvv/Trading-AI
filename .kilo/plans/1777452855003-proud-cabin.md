# Plan: Validation complète de l'intégration `tensortrade_model.py`

## Diagnostic actuel

### Chaîne d'intégration (validée)
```
tensortrade_model.py::get_tensortrade_prediction()
  ← enhanced_trading_example.py:42  (import)
  ← enhanced_trading_example.py:352 (appel, df passé en param)
  → résultat dict {"signal", "confidence"}
  → enhanced_decision_engine.py:413 (param de make_enhanced_decision)
  → enhanced_decision_engine.py:499-513 (création ModelDecision)
  → weight 0.10 dans base_weights (L267)
```

### Problèmes identifiés

| # | Problème | Sévérité |
|---|----------|----------|
| 1 | `test_tensortrade.py` (racine) est un stub inutile — n'importe même pas le module | HAUTE |
| 2 | `test_sb3.py` (racine) est un stub — juste un `print()` | HAUTE |
| 3 | Aucun test unitaire dans `tests/` pour `tensortrade_model.py` | HAUTE |
| 4 | Aucun test d'intégration bout-en-bout validant la chaîne complète | MOYENNE |
| 5 | Pas de benchmark de performance (latence PPO, consommation mémoire) | MOYENNE |
| 6 | Aucun ruff/pytest config dans `pyproject.toml` | BASSE |
| 7 | `fix_lint.py` (racine) patch `tensortrade_model.py` — dette technique | BASSE |

### Ce qui est correct
- Import bien présent dans `enhanced_trading_example.py:42`
- Appel bien présent à L352, résultat injecté dans le dict de prédictions
- `enhanced_decision_engine.py` reçoit bien le param `tensortrade_decision`
- Weight 0.10 configuré correctement
- Dépendances `tensortrade>=1.0.4` et `stable-baselines3>=2.8.0` dans `pyproject.toml`
- Le code lui-même (gym.Env + PPO) est fonctionnel et bien structuré

---

## Plan d'exécution

### Étape 1 — Nettoyage des stubs
- **Supprimer** `test_tensortrade.py` (racine) — stub inutile
- **Supprimer** `test_sb3.py` (racine) — stub inutile

### Étape 2 — Tests unitaires `tests/test_tensortrade_model.py` (nouveau fichier)
Suivre le pattern existant (`unittest` + `sys.path` + mocks). Tests à écrire :

| Test | Description |
|------|-------------|
| `test_insufficient_data` | `len(df) < 50` → retourne `{"signal": "HOLD", "confidence": 0.5}` |
| `test_missing_close_column` | DataFrame sans colonne Close → `{"signal": "HOLD", "confidence": 0.0}` |
| `test_lowercase_close_column` | DataFrame avec `close` (minuscule) → fonctionne |
| `test_successful_prediction_returns_valid_signal` | Mock PPO, vérifie signal ∈ {BUY, SELL, HOLD} et 0 ≤ confidence ≤ 1 |
| `test_env_step_logic` | Test unitaire du `SimpleTradingEnv` — reward correct pour buy/sell/hold |
| `test_env_observation_shape` | Vérifie shape de l'observation = (5,) |
| `test_env_reset` | Vérifie que reset() retourne obs + info dict |
| `test_exception_handling` | Force une exception → `{"signal": "HOLD", "confidence": 0.0}` |

**Stratégie de mock** : mocker `stable_baselines3.PPO` et `PPO.learn` pour les tests rapides, et avoir 1 test non-mocké (smoke test lent) marqué avec `@unittest.skipIf` ou un marker.

### Étape 3 — Test d'intégration `tests/test_tensortrade_integration.py` (nouveau fichier)
Valider la chaîne complète :
- Appeler `get_tensortrade_prediction()` avec un DataFrame réaliste (100+ rows)
- Passer le résultat à `EnhancedDecisionEngine.make_enhanced_decision()`
- Vérifier que le `ModelDecision` est bien créé avec `model_name="tensortrade"`
- Vérifier que le weight 0.10 est bien appliqué dans le score consensus

### Étape 4 — Benchmark complet `tests/bench_tensortrade.py` (nouveau fichier)
Mesurer (multi-size 100/500/1000 data points) :
- Temps d'entraînement PPO (500 timesteps) par size
- Temps de prédiction seule (env.reset + predict) par size
- Utilisation mémoire (avant/après model.learn) par size
- Reproductibilité (seed fixe → même résultat)
- Résumé : tableau markdown imprimé dans le terminal
- Durée estimée : ~5min

### Étape 5 — Ajout config ruff + pytest dans `pyproject.toml`
```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 120
target-version = "py312"
```

### Étape 6 — Suppression de `fix_lint.py` (dette technique)
Ce script one-shot a déjà été appliqué. À supprimer.

---

## Ordre d'exécution
1. Étape 5 (config ruff/pytest) — prerequisite
2. Étape 1 (nettoyage stubs)
3. Étape 6 (suppression fix_lint.py)
4. Étape 2 (tests unitaires — priorité maximale)
5. Étape 3 (test intégration)
6. Étape 4 (benchmark)
7. Lancer `pytest tests/` + `ruff check src/tensortrade_model.py` pour valider

## Fichiers modifiés/créés
- **Supprimés** : `test_tensortrade.py`, `test_sb3.py`, `fix_lint.py`
- **Modifiés** : `pyproject.toml` (ajout config pytest + ruff)
- **Créés** : `tests/test_tensortrade_model.py`, `tests/test_tensortrade_integration.py`, `tests/bench_tensortrade.py`
